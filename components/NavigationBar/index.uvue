<script lang="uts" setup>
	const emit = defineEmits(["leftClick", "rightClick"])

	const props = defineProps({
		title: {
			type: String,
			default: ''
		},
		showLeft: {
			type: Boolean,
			default: false
		},
		showRight: {
			type: Boolean,
			default: false
		}
	})

	// 左边点击
	const handleLeftClick = () => {
		emit("leftClick")
	}

	// 右边点击
	const handleRightClick = () => {
		emit("rightClick")
	}
</script>

<template>
	<view class="navigation-container">
		<view class="status-bar"></view>
		<view class="navigation-bar">
			<view class="back-container flex-center">
				<view class="back-btn" hover-class="back-btn-hover" v-if="props.showLeft" @tap="handleLeftClick">
					<slot name="left">
						<uni-icons color="#fff" type="left" size="30"></uni-icons>
					</slot>
				</view>
			</view>
			<text class="title">{{ props.title }}</text>
			<view class="back-container flex-center">
				<view class="back-btn" hover-class="back-btn-hover" v-if="props.showRight" @tap="handleRightClick">
					<slot name="right">
						<uni-icons color="#fff" type="right" size="30"></uni-icons>
					</slot>
				</view>
			</view>
		</view>
	</view>
</template>

<style scoped lang="scss">
	.navigation-container {
		background-color: $navBgColor;

		.status-bar {
			height: var(--status-bar-height);
		}
	}

	.back-btn-hover {
		background-color: rgba(255, 255, 255, 0.4);
	}

	.navigation-bar {
		height: 44px;
		padding: 0 10px;
		display: flex;
		flex-direction: row;
		align-items: center;
		justify-content: space-between;

		.back-container {
			width: 40px;
			height: 40px;
		}

		.back-btn {
			width: 40px;
			height: 40px;
			background-color: transparent;
			border-radius: 20px;
			align-items: center;
			justify-content: center;
		}

		.title {
			flex: 1;
			text-align: center;
			color: $navTextColor;
		}
	}
</style>
