import type { IGroupItem } from "./index"

// 主mainStore
export type IMainStore = {
  currentTab: string,
  groupList: IGroupItem[],
  redirectUri: string,
  accessToken: string,
  refreshToken: string,
  expiresIn: number,
}